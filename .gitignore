# Service account keys. These are real secrets — they must never be committed.
*serviceaccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
firebase-key.json
.env

# Python leaves these behind; never commit them
__pycache__/
*.pyc

# the site's base data is members-only: it lives in Firestore (site/data),
# uploaded with scripts/publish_data.py - never in this public repo
data.json
