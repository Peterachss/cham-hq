# Service account keys. These are real secrets — they must never be committed.
*serviceaccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
firebase-key.json
.env

# Python leaves these behind; never commit them
__pycache__/
*.pyc
